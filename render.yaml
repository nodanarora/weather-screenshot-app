services:
  - type: web
    name: weather-screenshot-app
    env: node
    plan: starter
    buildCommand: |
      cd frontend && npm install --legacy-peer-deps && npm run build && cd ../backend && npm install --legacy-peer-deps
    startCommand: node server.js
    envVars: []
